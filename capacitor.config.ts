import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ns.schoolassistant',
  appName: 'SchoolAssistant',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
