import { PluspointsFormatterPipe } from './pluspoints-formatter.pipe';

describe('PluspointsFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new PluspointsFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
